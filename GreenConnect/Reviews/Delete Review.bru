meta {
  name: Delete Review
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/reviews/{{reviewId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

tests {
  test("should delete review successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.have.property('success');
    expect(res.getBody().success).to.be.true;
  });
}