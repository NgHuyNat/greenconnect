meta {
  name: Get Seller Orders
  type: http
  seq: 2
}

get {
  url: http://localhost:3001/api/v1/orders/seller
  body: none
  auth: bearer
}

params:query {
  status: 
  page: 1
  limit: 10
}

auth:bearer {
  token: {{authToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response is array of orders", function() {
    expect(res.getBody()).to.be.an('array');
  });
}